name: Exécuter mon code

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Exécuter code
      run: echo "Hello World ! Votre code s'exécute ici"

